#lang info

;; sha2-kit — SHA-256 and HMAC-SHA256 over libcrypto's EVP interface.
;; Standalone Racket package: no app knowledge, publishable as-is.
(define collection "sha2-kit")
(define version "0.1.0")
(define deps '("base"))
(define pkg-desc "SHA-256 and HMAC-SHA256 for Racket, over libcrypto")
